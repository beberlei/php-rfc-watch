{% extends "base.html.twig" %}

{% block content %}
    <h1 class="text-xl font-bold">Admin: {{ rfc.title }}</h1>

    <input class="p-2 mb-8 shadow rounded" style="width: 100%;" value="PHP RFC {% if rfc.status == "rejected" %}Rejected{% else %}Accepted{% endif %}: {{ rfc.title }}" />

    <textarea class="p-2 shadow rounded" style="width: 100%; height: 300px;">Hey there fellow PHP RFC Watcher,

- **RFC:** [{{ rfc.title }}]({{ rfc.url }})
- **Discussions:** {% for discussion in rfc.discussions %}[#{{ loop.index }}]({{ discussion }}){% if not loop.last %}, {% endif %}{% endfor %}

- **Target:** {{ rfc.targetPhpVersion }}
- **Status:** {% if rfc.status == "rejected" %}Rejected{% else %}Accepted{% endif %}


**Votes**:
{% for vote in rfc.votes if vote.primaryVote %}
- **{{ vote.question }}** {% for answer,count in vote.currentVotes %}{{ count }} {{ answer }}{% if not loop.last %}, {% endif %}{% endfor %}
{% endfor %}

    </textarea>

{% endblock %}
